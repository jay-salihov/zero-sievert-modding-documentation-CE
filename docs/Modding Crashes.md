# Modding Crashes

When mods fail to compile/build a crash report is generated and put into `%localappdata%/ZERO_Sievert`. It's title is the current date and it will generally contain the `// Trace Error` and the `// Stack Trace`.

The most useful error is the `Trace Error`. It will generally contain (assuming the error isn't syntax related) a very clear error report for you.